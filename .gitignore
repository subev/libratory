node_modules/
dist/
.env
# Any sibling of it too: a .env.backup holding live keys is one `git add -A` from a public repo
.env.*
!.env.example
.venv/
.venv-pocket/
data/
*.tsbuildinfo
.turbo/
.DS_Store
__pycache__/
.claude/
storyteller/STORYTELLER_SECRET_KEY.txt
storyteller/ADMIN_CREDENTIALS.txt

*.mov

e2e/test-results/
# Playwright writes here when it is run from a package rather than e2e/, and it is where
# throwaway screenshots land — never anything the repo should carry.
**/test-results/
e2e/playwright-report/
docker-compose.override.yml
.models-missing
.uv/
.bun/

# wrangler local state (get/)
.wrangler/
/vivliostyle/
